#!/bin/bash

retcode=-1
while (($retcode != 0)); do
  mysql -u$"${QLINK_DB_USERNAME}" -h"${QLINK_DB_HOST}" -P3306 -Dqlink --password="${QLINK_DB_PASSWORD}" < ./schema.sql
  retcode=${?}
  sleep 1
done

./run
